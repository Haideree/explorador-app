function ReactInfo() {

  return (

    <div
      style={{
        background: "white",
        padding: "15px",
        borderRadius: "10px",
        marginBottom: "20px"
      }}
    >

      <h2>¿Qué es React?</h2>

      <p>
        React es una librería de JavaScript creada por Meta
        que permite construir interfaces de usuario mediante
        componentes reutilizables y dinámicos.
      </p>

    </div>

  );

}

export default ReactInfo;